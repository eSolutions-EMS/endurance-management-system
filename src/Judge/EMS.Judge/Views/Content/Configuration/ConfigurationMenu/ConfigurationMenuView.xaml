<UserControl x:Class="EMS.Judge.Views.Content.Configuration.ConfigurationMenu.ConfigurationMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:localization="clr-namespace:Core.Localization;assembly=Core.Localization"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel Style="{StaticResource List-Vertical}">
        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding Event}"
                Content="{x:Static localization:Strings.ENDURANCE_EVENTS_ENTITY}" />

        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding AthleteList}"
                Content="{x:Static localization:Strings.ATHLETES_ENTITY}" />

        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding HorseList}"
                Content="{x:Static localization:Strings.HORSES_ENTITY}" />

        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding ParticipantList}"
                Content="{x:Static localization:Strings.PARTICIPANTS_ENTITY}" />
    </StackPanel>
</UserControl>

@inherits LayoutComponentBase

<div class="page">
    <ErrorHandlerComponent @ref="this.errorBoundary">
        <ChildContent>
            <div class="sidebar">
                <NavMenu />
            </div>

            <main>
                <article class="content px-4">
                    @Body
                </article>
            </main>
        </ChildContent>

        <ErrorContent>
            <div class="container">
                <p>@this.errorBoundary!.Exception!.Message</p>
                <p>@this.errorBoundary!.Exception!.StackTrace</p>
            </div>
            <button class="btn btn-primary" @onclick="_ => this.errorBoundary?.Recover()">Back</button> 
        </ErrorContent>
    </ErrorHandlerComponent>
</div>

<ToasterComponent />

@code {
    private ErrorHandlerComponent? errorBoundary;
}

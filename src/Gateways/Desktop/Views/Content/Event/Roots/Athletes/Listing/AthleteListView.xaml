<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Views.Content.Event.Roots.Athletes.Listing.AthleteListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:strings="clr-namespace:EnduranceJudge.Localization;assembly=EnduranceJudge.Localization"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel Style="{StaticResource Wrapper}">
        <Button Style="{StaticResource Button}"
                Command="{Binding ChangeToCreate}"
                Content="{x:Static strings:DesktopStrings.CreateButtonText}" />

        <ListBox Style="{StaticResource List}"
                 ItemsSource="{Binding ListItems}" />
    </StackPanel>
</UserControl>

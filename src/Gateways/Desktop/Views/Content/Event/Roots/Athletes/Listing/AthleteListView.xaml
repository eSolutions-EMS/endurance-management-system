<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Views.Content.Event.Roots.Athletes.Listing.AthleteScrollableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:strings="clr-namespace:EnduranceJudge.Localization;assembly=EnduranceJudge.Localization"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource Text-H1}"
                   Text="{x:Static strings:DesktopStrings.AthletesListTitle}" />

        <DockPanel Style="{StaticResource Stretch-Last}"
                   Grid.Row="1">
            <Button Style="{StaticResource Button}"
                    Command="{Binding ChangeToCreate}"
                    Content="{x:Static strings:DesktopStrings.CREATE_BUTTON}" />
        </DockPanel>

        <DockPanel Style="{StaticResource Stretch-Last}"
                   Grid.Row="2">
            <Button Style="{StaticResource Button}"
                    Command="{Binding ClearSearch}"
                    Content="{x:Static strings:DesktopStrings.ClearButtonText}" />

            <Button Style="{StaticResource Button}"
                    Command="{Binding Search}"
                    Content="{x:Static strings:DesktopStrings.SearchButtonText}" />

            <TextBox Style="{StaticResource Input}"
                     Text="{Binding SearchValue}" />
        </DockPanel>

        <DockPanel Style="{StaticResource Stretch-Last}"
                   Grid.Row="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          CanContentScroll="True"
                          PreviewMouseWheel="HandleScroll">
                <ListBox Style="{StaticResource ListBox-Vertical}"
                         ItemsSource="{Binding ListItems}" />
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>

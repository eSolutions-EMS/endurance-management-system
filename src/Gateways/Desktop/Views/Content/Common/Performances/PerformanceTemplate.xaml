<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:EnduranceJudge.Gateways.Desktop.Views.Content.Common.Performances"
                    xmlns:localization="clr-namespace:EnduranceJudge.Localization;assembly=EnduranceJudge.Localization"
                    xmlns:xaml="clr-namespace:EnduranceJudge.Gateways.Desktop.Core.Components.XAML">

    <DataTemplate DataType="{x:Type local:PerformanceTemplateModel}">
        <xaml:ScalableStackPanel Style="{StaticResource Performance-Column}" Margin="0 -3px 0 0" Name="Kur">
            <!-- Header -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding HeaderValue}" />
            </xaml:ScalableBorder>
            <!-- EDITABLE TIMES -->
            <!-- Arrival Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding EditVisibility}">
                <TextBox Style="{StaticResource TextBox-Table}"
                         Text="{Binding ArrivalTimeString}" BorderThickness="0" />
            </xaml:ScalableBorder>
            <!-- Inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding EditVisibility}">
                <TextBox Style="{StaticResource TextBox-Table}"
                         Text="{Binding InspectionTimeString}" BorderThickness="0" />
            </xaml:ScalableBorder>
            <!-- ReInspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding EditVisibility}">
                <TextBox Style="{StaticResource TextBox-Table}"
                         Text="{Binding ReInspectionTimeString}" BorderThickness="0" />
            </xaml:ScalableBorder>
            <!-- Required Inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding EditVisibility}">
                <TextBox Style="{StaticResource TextBox-Table}"
                         Text="{Binding RequiredInspectionTimeString}" />
            </xaml:ScalableBorder>
            <!-- Compulsory Required Inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding EditVisibility}">
                <TextBox Style="{StaticResource TextBox-Table-Bold}"
                         Text="{Binding CompulsoryRequiredInspectionTimeString}" />
            </xaml:ScalableBorder>
            <!-- END EDITABLE TIMES -->
            <!-- READONLY TIMES -->
            <!-- Arrival Time-->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding ReadonlyVisibility}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding ArrivalTimeString}" />
            </xaml:ScalableBorder>
            <!-- Inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding ReadonlyVisibility}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding InspectionTimeString}" />
            </xaml:ScalableBorder>
            <!-- Re-inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding ReadonlyVisibility}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding ReInspectionTimeString}" />
            </xaml:ScalableBorder>
            <!-- Required Inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding ReadonlyVisibility}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding RequiredInspectionTimeString}" />
            </xaml:ScalableBorder>
            <!-- Compulsory Required Inspection Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding ReadonlyVisibility}">
                <xaml:ScalableTextBlock Style="{StaticResource Text-Bold}"
                         Text="{Binding CompulsoryRequiredInspectionTimeString}" />
            </xaml:ScalableBorder>
            <!-- END READONLY TIMES -->
            <!-- Next Performance Start Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}">
                <xaml:ScalableTextBlock Style="{StaticResource Text-Bold}"
                           Text="{Binding NextStartTimeString, Mode=OneWay}" />
            </xaml:ScalableBorder>
            <!-- Recovery Span -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding RecoverySpanString, Mode=OneWay}" />
            </xaml:ScalableBorder>
            <!-- Time -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}"
                           Text="{Binding TimeString, Mode=OneWay}" />
            </xaml:ScalableBorder>
            <!-- Average Speed Loop -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}">
                    <Run Text="{Binding AverageSpeedString, Mode=OneWay}" />
                    <Run Text="{x:Static localization:Strings.KM_PER_HOUR}" />
                </xaml:ScalableTextBlock>
            </xaml:ScalableBorder>
            <!-- Average Speed Total -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}">
                <xaml:ScalableTextBlock Style="{StaticResource Text}">
                    <Run Text="{Binding AverageSpeedTotalString, Mode=OneWay}" />
                    <Run Text="{x:Static localization:Strings.KM_PER_HOUR}" />
                </xaml:ScalableTextBlock>
            </xaml:ScalableBorder>
            <!-- Edit button -->
            <xaml:ScalableBorder Style="{StaticResource Border-Table-Cell}" Visibility="{Binding EditVisibility}">
                <Button Style="{StaticResource Button-Table}"
                        Command="{Binding Edit}"
                        Content="{x:Static localization:Strings.EDIT}" />
            </xaml:ScalableBorder>
        </xaml:ScalableStackPanel>
    </DataTemplate>
</ResourceDictionary>

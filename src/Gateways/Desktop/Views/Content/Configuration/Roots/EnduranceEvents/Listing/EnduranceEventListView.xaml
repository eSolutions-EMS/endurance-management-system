<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Views.Content.Configuration.Roots.EnduranceEvents.Listing.EnduranceEventListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:strings="clr-namespace:EnduranceJudge.Localization;assembly=EnduranceJudge.Localization"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel Style="{StaticResource Wrapper}">
        <TextBlock Style="{StaticResource Text-H1}"
                   Text="{x:Static strings:DesktopStrings.EnduranceEventListTitle}" />

        <Button Style="{StaticResource Button-Form-Create}"
                Command="{Binding ChangeToCreate}" />

        <ListBox Style="{StaticResource ListBox-Vertical}"
                 ItemsSource="{Binding EnduranceEvents}" />
    </StackPanel>
</UserControl>

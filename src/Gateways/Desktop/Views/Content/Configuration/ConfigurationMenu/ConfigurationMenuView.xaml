<UserControl x:Class="EnduranceJudge.Gateways.Desktop.Views.Content.Configuration.ConfigurationMenu.ConfigurationMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:localization="clr-namespace:EnduranceJudge.Localization.Translations;assembly=EnduranceJudge.Localization"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <StackPanel Style="{StaticResource List-Vertical}">
        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding Event}"
                Content="{x:Static localization:Entities.ENDURANCE_EVENTS}" />

        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding AthleteList}"
                Content="{x:Static localization:Entities.ATHLETES}" />

        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding HorseList}"
                Content="{x:Static localization:Entities.HORSES}" />

        <Button Style="{StaticResource Button-Big-Vertical}"
                Command="{Binding ParticipantList}"
                Content="{x:Static localization:Entities.PARTICIPANTS}" />
    </StackPanel>
</UserControl>

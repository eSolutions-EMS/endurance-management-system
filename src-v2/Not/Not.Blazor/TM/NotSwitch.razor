@using Not.Blazor.Components

@inherits BindableValueComponent<bool>

<MudSwitch T="bool" @bind-Value="Value" Label="@Localizer.Get(Label)"
           ThumbIcon="@(Value==true ? Icons.Material.Filled.Done : Icons.Material.Filled.Close)"
           ThumbIconColor="@(Value==true ? Color.Success : Color.Error)" />

@code {
    [Parameter]
    public string Label { get; set; } = default!;
}